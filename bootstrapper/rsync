#sync folders
sudo mkdir -p /opt/cluster-config
sudo chown core /opt/cluster-config
while true ; do rsync -Pza 18680666@10.10.0.1:/Users/alexisvincent/Code/steve-cluster/ /opt/cluster-config  && sleep 5; done
